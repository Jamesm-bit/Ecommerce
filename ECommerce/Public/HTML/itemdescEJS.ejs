<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Furth Pattern</title>
    <script src="https://use.fontawesome.com/releases/v5.0.8/js/all.js"></script>
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.12.9/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js"></script>
    <link href="/CSS/style.css" rel="stylesheet">
</head>

<body>
    <div class="container-fluid navbar sticky-top headbar">
        <button onclick="goHome()">Go Back</button>
    </div>
    <div class="container-fluid descitems">
        <%for(item in initems) {%>
            <div class="<%=initems[item]._id%> container-fluid" id="itemDescription">
                <div id="itemDescHolder">
                    <div class="itemfeilds">
                        <p class="name listobj">
                            Name:<%= initems[item].name %>
                        </p>
                        <p class="description listobj">
                            Description: <%=initems[item].desc%>
                        </p>
                        <p class="price listobj">
                            Price: $<%=initems[item].price%>
                        </p>
                    </div>
                    <img class="image" src="data:image/jpeg;base64,<%=initems[item].img%>">
                </div>
                <div id="editorHolder">
                    <form action="/update" enctype="multipart/form-data" method="POST" class="itemInput">
                        <input type="text" class="updateID" name="id" id="id" value="<%=initems[item]._id%>">
                        <div class="editholder">
                            <input class="editfeild" type="text" name="name" id="name" placeholder="Name">
                            <input class="editfeild" type="text" name="desc" id="desc" placeholder="Description">
                            <input class="editfeild" type="text" name="price" id="price" placeholder="Price">
                            <input class="editfeild" type="file" name="MyImage" id="image" name="MyImage" accept="image/*">
                            <button type="submit" id="<%=initems[item]._id%>">Edit</button>
                        </div>
                        <button type="button" id="<%=initems[item]._id%>" onclick="delItem(this.id)">Delete</button>
                    </form>
                </div>
            </div>
            <%}%>
    </div>


    <script src="../Javascript/itemdesc.js"></script>
</body>